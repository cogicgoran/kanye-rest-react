import React from 'react';
import Header from '../../components/header/Header';

function Weather() {
  return (
    <div>
        <Header />
        <h2>Weather</h2>
    </div>
  );
};

export default Weather;